# Where the app loads the dataset from. Optional — defaults to the data repo
# via jsDelivr. Override to point at a local copy during development, e.g.:
# VITE_DATA_URL="http://localhost:8000/prices.csv"
VITE_DATA_URL=""
